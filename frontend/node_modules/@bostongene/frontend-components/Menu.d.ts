import { PureComponent } from 'react';
import { IOption } from './Helpers';

export interface IMenuOption extends IOption {
  isDisabled?: boolean;
}

export interface IMenuProps {
  hasTitleAttribute?: boolean;
  isUsedInSelect?: boolean;
  onChange: (value: string, selectedIdx: number) => void;
  options: IMenuOption[];
  selectedIdx?: number;
}

declare class Menu extends PureComponent<IMenuProps> {}

export default Menu;
